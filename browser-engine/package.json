{
  "name": "routex-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @routex/web dev",
    "build": "pnpm --filter @routex/browser-engine build && pnpm --filter @routex/web build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}