# Development helpers

- `dev.sh` — start the API (SQLite fallback) and the Next.js dev server together.
- `seed_db.py` (in `../seed`) — seed scenarios into the database explicitly.

For benchmarking without a server, see `../benchmarking/run_benchmark.py`.
